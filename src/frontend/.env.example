# 开发环境 - 本地开发时使用
VITE_API_URL=http://localhost:5000/api

# 生产环境 - Docker部署时，将会被docker-compose.yml中的环境变量覆盖
# VITE_API_URL=http://backend:5000/api

# 如果部署到特定服务器，请修改为实际的服务器地址
# VITE_API_URL=http://your-server-ip:5000/api
